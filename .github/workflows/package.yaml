# Copyright 2026 Enactic, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Package
on:
  push:
    branches-ignore:
      - 'copilot/**'
      - 'dependabot/**'
    tags:
      - '**'
  pull_request:
concurrency:
  group: ${{ github.head_ref || github.sha }}-${{ github.workflow }}
  cancel-in-progress: true
jobs:
  source:
    name: Source
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Prepare environment variables
        run: |
          PACKAGE_NAME=$(yq --unwrapScalar .project.name app/pyproject.toml)
          echo "PACKAGE_NAME=${PACKAGE_NAME}" >> "${GITHUB_ENV}"
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            echo "VERSION=${GITHUB_REF_NAME}" >> "${GITHUB_ENV}"
          else
            VERSION=$(yq --unwrapScalar .project.version app/pyproject.toml)
            echo "VERSION=${VERSION}" >> "${GITHUB_ENV}"
          fi
      - name: Create source archive
        run: |
          git archive HEAD \
            --output=${PACKAGE_NAME}-${VERSION}.tar.gz \
            --prefix=${PACKAGE_NAME}-${VERSION}/
          sha256sum \
            ${PACKAGE_NAME}-${VERSION}.tar.gz > \
            ${PACKAGE_NAME}-${VERSION}.tar.gz.sha256
          sha512sum \
            ${PACKAGE_NAME}-${VERSION}.tar.gz > \
            ${PACKAGE_NAME}-${VERSION}.tar.gz.sha512
      - uses: actions/upload-artifact@v7
        with:
          name: source
          path: |
            ${{ env.PACKAGE_NAME }}-${{ env.VERSION }}.tar.gz
            ${{ env.PACKAGE_NAME }}-${{ env.VERSION }}.tar.gz.sha256
            ${{ env.PACKAGE_NAME }}-${{ env.VERSION }}.tar.gz.sha512
  release:
    name: Release
    if: github.ref_type == 'tag'
    needs:
      - source
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: release
    permissions:
      contents: write
      discussions: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: source
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version=${GITHUB_REF_NAME}
          package_name=$(yq --unwrapScalar .project.name app/pyproject.toml)
          gh release create ${version} \
            --discussion-category Announcements \
            --generate-notes \
            --repo ${GITHUB_REPOSITORY} \
            --title "${package_name} ${version}" \
            --verify-tag \
            *.tar.gz*
