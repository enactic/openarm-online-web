[project]
name = "OpenEval website"
version = "0.1.0"
description = "This is the OpenEval website."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.128.8",
]
